type PropertyValue = string | number | boolean | null | string[] | number[]

export type IdentifyProperties = Record<string, PropertyValue>

export type CaptureProperties = Record<string, PropertyValue>

export type ScreenProperties = Record<string, PropertyValue>
