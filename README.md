# TOLO

Monorepo containing the following applications:

- `apps/app`: Expo app
- `apps/poster-plugin`: Poster plugin for POS system
- `apps/website`: Website built with React Router
- `apps/studio`: Sanity Studio for content management
- `apps/workers`: Cloudflare Workers for hooks, handlers and background tasks
- `packages/common`: Shared utilities and types
