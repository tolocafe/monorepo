/**
 * Site Constants
 *
 * Central location for site-wide configuration values.
 * Using www subdomain as canonical to consolidate SEO value.
 */

/** Canonical base URL for the website (with www) */
export const BASE_URL = 'https://www.tolo.cafe'

/** Organization schema ID for structured data consistency */
export const ORGANIZATION_ID = `${BASE_URL}/#organization`
